//
//  Extension + UILabel.swift
//  HW Lesson 3.2
//
//  Created by Елизавета Медведева on 25.07.2023.
//

import UIKit
/*
extension UILabel {
    
    func description(with: ) -> String {
                        """
                        preset: \(name)
                        curve: \(curve)
                        force: \(String(format: "%.02f", force))
                        duration: \(String(format: "%.02f", duration))
                        delay: \(String(format: "%.02f", delay))
                        """
    }
    
    var description: String {
                """
                preset: \(name)
                curve: \(curve)
                force: \(String(format: "%.02f", force))
                duration: \(String(format: "%.02f", duration))
                delay: \(String(format: "%.02f", delay))
                """
    }
}
*/
